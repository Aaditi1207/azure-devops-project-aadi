trigger:
- main

variables:
  imageName: demo-app

stages:
- stage: Build
  jobs:
  - job: BuildPush
    pool:
      name: self-hosted-linux
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'aadiacr'
        repository: $(imageName)
        command: buildAndPush
        Dockerfile: Dockerfile
        tags: latest

- stage: Deploy
  dependsOn: Build
  jobs:
  - job: Deploy
    pool:
      name: self-hosted-linux
    steps:
    - task: KubernetesManifest@1
      inputs:
        action: deploy
        kubernetesServiceConnection: 'aadiaks-default'
        manifests: k8s-deployment.yaml